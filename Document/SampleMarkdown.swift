import AppKit
import SwiftUI

/// 新文档的默认示例（打开已有文件时会被内容整体覆盖；M1 后移入欢迎页）。
public enum SampleMarkdown {
    public static let text = """
    # Muse · M0 技术验证

    这是一份用于验证「即时渲染」的示例文档。原则：**渲染只写属性，不改字符**。

    ## 行内样式

    - **粗体**、*斜体*、~~删除线~~ 与 `行内代码`，中文 English 混排：**加粗中文**正常。
    - 链接：[Apple 官网](https://www.apple.com) 与嵌套标签 [a [b] c](https://example.com)。
    - emoji 与 ZWJ 序列：😀 🚀 🇨🇳 👨‍👩‍👧‍👦
    - 嵌套：**粗体里的 *斜体* 与 `代码`**，以及 ***三层星号***
    - 未闭合的粗体 **这里没有闭合，标记应保持可见
    - 转义：\\*不是强调\\*，反斜杠是字面量。
    - 空行、多行段落，以及超长行的自动换行验证：这一行要足够长，长到超过窗口宽度触发软换行，用来观察 marker 隐藏后是否影响换行与光标移动。

    ## 列表与任务

    1. 有序列表第一项
    2. 有序列表第二项
       - 无序嵌套：这是子项，缩进两个空格
       - 再次：zài cì 再次

    - [x] 已完成任务：验证零宽 marker
    - [ ] 未完成任务：验证 IME

    ## 引用与代码

    > 引用块：渲染只写属性，不改字符。中文引用的排版需要观察。

    ```swift
    func hello() -> String {
        "Hello, Muse" // 代码块内的中文宽度与对齐
    }
    ```

    ---

    末段：方向键穿越隐藏 marker、跨行选择、鼠标点击命中，都需要人工验证。
    """
}
